Small userscript to replace Gmail's standard favicon with a custom favicon containing the number of unread messages.

PSD file is provided so you can make your own images if you'd like to.

To install, simply drag the .js file to your browser if you're using Google Chrome, or install the Greasemonkey extension first if you're using Firefox.
